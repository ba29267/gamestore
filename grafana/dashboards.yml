# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: "GameStore Dashboards"
    orgId: 1
    folder: "GameStore"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
